## Cara Menjalankan Dashboard (Local)
1. Buat virtual environment python -m venv venv
2. Aktifkan virtual environment venv\Scripts\activate (Windows)
3. Install dependencies: pip install -r requirements.txt
4. Masuk ke folder dashboard: cd dashboard
5. Jalankan dashboard (local): python -m streamlit run dashboard.py
